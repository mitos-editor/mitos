# `view`

Owns the shared editor model and behavior: documents, views, editor state, editing and save operations, and editor configuration. Coordinates editor features, background work, and protocol results across frontends.
