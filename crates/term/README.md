# `term`

Owns the terminal application: startup, configuration composition, the event loop, keybindings, prompts, and interactive UI. Adapts terminal input to shared editor operations and presents their results.
